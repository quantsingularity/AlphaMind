# ==============================================================================
# AlphaMind Environment Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Core Application Settings
# ------------------------------------------------------------------------------
# A secret key for the Flask application and session management.
SECRET_KEY="your_flask_secret_key_here"

# A secret key for signing JSON Web Tokens (JWTs) for user authentication.
JWT_SECRET="your_jwt_signing_secret_here"

# General API Key for service-to-service communication or external access.
API_KEY="your_general_api_key_here"

# Application logging level (e.g., DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL="INFO"

# Time-to-live for cache entries in seconds (e.g., 3600 seconds = 1 hour)
CACHE_TTL="3600"


# ------------------------------------------------------------------------------
# 2. Database Configuration (Used by backend/src/main.py)
# ------------------------------------------------------------------------------
# The database connection string uses mysql+pymysql, so these are for a MySQL-compatible database.
DB_HOST="localhost"
DB_PORT="3306"
DB_NAME="alphamind"
DB_USERNAME="root"
DB_PASSWORD="password"


# ------------------------------------------------------------------------------
# 3. Market Data API Keys
# ------------------------------------------------------------------------------
# Specific API keys mentioned in the repository.
ALPHAVANTAGE_API_KEY="your_alphavantage_api_key"
POLYGON_API_KEY="your_polygon_api_key"

# Generic structure for other API connectors (used in base.py).
# Replace {PREFIX} with the name of the service (e.g., BINANCE, ALPACA, IEX).
# {PREFIX}_API_KEY="your_api_key"
# {PREFIX}_API_SECRET="your_api_secret"
# {PREFIX}_USERNAME="your_username"
# {PREFIX}_PASSWORD="your_password"
# {PREFIX}_TOKEN="your_token"
# {PREFIX}_ADDITIONAL_PARAMS='{"key": "value"}'


# ------------------------------------------------------------------------------
# 4. Frontend Configuration (Used by web-frontend/ or mobile-frontend/)
# ------------------------------------------------------------------------------
# The base URL for the backend API (e.g., http://localhost:5000/api/v1)
API_URL="http://localhost:5000/api/v1"

# The base URL for the WebSocket connection (e.g., ws://localhost:5000/ws)
WS_URL="ws://localhost:5000/ws"
